{
  "mcpServers": {
    "github": {
      "type": "http",
      "url": "https://api.githubcopilot.com/mcp/",
      "note": "GitHub API — PR management, issue tracking, code search, repository operations"
    },
    "sentry": {
      "type": "http",
      "url": "https://mcp.sentry.dev/sse",
      "note": "Sentry — error monitoring, issue tracking, release health"
    },
    "firebase-firestore": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "mcp-firestore"],
      "env": {
        "SERVICE_ACCOUNT_KEY_PATH": "${GOOGLE_APPLICATION_CREDENTIALS}",
        "FIRESTORE_PROJECT_ID": "${FIREBASE_PROJECT_ID}"
      },
      "note": "Firestore — direct database queries, schema inspection, document management"
    },
    "postgres": {
      "type": "stdio",
      "command": "npx",
      "args": ["-y", "@bytebase/dbhub", "--transport", "stdio"],
      "env": {
        "DB_URL": "${DATABASE_URL}"
      },
      "note": "PostgreSQL — database queries, schema inspection, migration support"
    }
  }
}
